# RELDEC runtime dependencies (Python >= 3.10)
# Install from Decoder repo root with:
#   python3 -m pip install -r RELDEC/requirements.txt

numpy>=1.26
scipy>=1.11
torch>=2.2

# Local LDPC package in this repository.
-e ../ldpc
